{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "CHANGED=$(echo \"$TOOL_INPUT\" | grep -oP '\"file_path\":\\s*\"\\K[^\"]+' 2>/dev/null || true); if [ -n \"$CHANGED\" ] && echo \"$CHANGED\" | grep -qE '\\.(tsx?|jsx?)$'; then npx eslint \"$CHANGED\" --fix --quiet 2>/dev/null || true; fi"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(npm run build)",
      "Bash(npm run lint*)",
      "Bash(npm run dev)",
      "Bash(npm run validate*)",
      "Bash(npx tsc*)",
      "Bash(npx eslint*)",
      "Bash(git *)"
    ]
  }
}
