# W3 Round 3 — Final External Critic (2026-05-18)

**Subagent**: ab4ea12c7f58d9e86 (general-purpose、43 秒、37k tokens)
**Target**: `w3-r2-draft2-20260518.md`
**Verdict**: 🟡 YELLOW — ship 不可、RED 2 + YELLOW 4 を Draft 3 で必修

## RED (Draft 3 で必修)

### R-4 /editorial-policy stub thin content
各 1 段落 stub は AdSense reviewer の "low value programmatic page" flag 再現 risk。Round 2 で flag 済だが Draft 2 で stub 据え置きは regression。
**修正**: 各 section ≥ 150 words 拡張 + 具体例 (e.g. Visit policy = 「全 venue 私が現地撮影、unable to visit は明記」+ 直近 3 例 link)、または /editorial-policy/ を Commit B から **除外** して別 PR で W4 投入。

### R-10 AdSense reject letter slug 未取得 = blind spot
W18 reject 主因が identity flip ではなく content quality (LOW_DENSITY 等) だった場合、W3 投入の効果が AdSense 再審査直結しない。
**修正**: user に reject letter slug 取得 W3 ship 前 gate 化。取得不能なら W3 を **EEAT 単独施策**として ship、AdSense 再審査は W4 以降に分離 communicate。

## YELLOW

### Y-1 PR squash merge 設定
Vercel が commit 単位 deploy queue で Commit A → Commit B 間 short window で 3 routes 404、sitemap miss。
**修正**: PR 設定 = "Squash and merge"、`.vercel/project.json` `git.deploymentEnabled` main branch のみ true 確認。

### Y-2 LinkedIn URL CI gate
`takashi-kiyohara111222` 末尾 1-1-1-2-2-2 手打ち事故 risk。
**修正**: `const LINKEDIN_VANITY = "takashi-kiyohara111222"` で抽出、CI で `curl -A "Mozilla/5.0" https://www.linkedin.com/in/${LINKEDIN_VANITY}` を 200 fetch verify gate 化。

### Y-6 rel="me" Indieweb reciprocal
LinkedIn 側プロフィールに reciprocal link (japan-pop-now.com URL) 無いと sameAs 補強効果は schema 経由のみ。
**修正**: LinkedIn Contact info に "https://www.japan-pop-now.com/about/takashi-kiyohara/" 追加を user manual task として handoff doc 記載。

### Y-9 rollback re-index 計画
URL Inspection 50/日 quota、56 articles 全体は不可能。
**修正**: rollback 時は 3 新 routes + /about/ + homepage 計 5 URL のみ手動 re-index、article 群は IndexNow plugin に委譲。

## GREEN

- 項目 3 /about TEAM overlap <20% ✓
- 項目 5 Person.image 既存 .webp 内容据え置きで OK ✓
- 項目 7 Vercel preview deploy 検証手順 ✓
- 項目 8 sitemap lastmod 真改稿のみ ✓

## ★ Draft 3 ship 版 changelog

- [P0] /editorial-policy/ 各 section 150+ words 拡張、または Commit B から除外
- [P0] AdSense reject letter slug user 取得 → 取得不能時 W3 を EEAT 単独 frame 化
- [P1] PR squash merge 設定 + Vercel preview gate
- [P1] LinkedIn URL CI fetch 200 gate + LinkedIn 側 reciprocal link manual task
- [P1] rollback re-index 計画 = 5 URL 手動 + IndexNow article

## ★ Code 投入直前 Cowork checklist (Draft 3 finalize 後)

1. `lib/author.ts` AUTHOR.socials.linkedin = `https://www.linkedin.com/in/takashi-kiyohara111222` を HTTP 200 fetch (curl + Googlebot UA) で verify
2. /editorial-policy/ 3 sections 各 150+ words 確認 (`wc -w` gate)
3. `grep -rE "Takapon" --include="*.{ts,tsx,md,mdx,json}" --exclude-dir={.git,node_modules,.next,docs,scripts/audit}` 0 hit verify
4. PR settings = "Squash and merge"、Co-Authored-By なし
5. Vercel preview URL で /about/takashi-kiyohara/ Person JSON-LD を Schema.org validator pass + AuthorBox LinkedIn icon click で LinkedIn profile 遷移確認
