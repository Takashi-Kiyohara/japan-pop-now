# W3 — Identity Flip Code Prompt **Draft 3 = SHIP 版** (2026-05-18)

**Status**: 🚢 SHIP 版 (Round 1+2+3 完走、AdSense slug 取得済受け R-10 close)
**Round 1 input**: `w3-r1-research-20260518.md`
**Round 2 input**: `w3-r2-critic-20260518.md` + `w3-r2-codebase-audit-20260518.md`
**Round 3 input**: `w3-r3-critic-20260518.md`
**Frame**: ★ **EEAT 単独補強** (AdSense 直接 fix ではない、reject 主因「有用性の低いコンテンツ」は W5+W2 で fix)

---

## Advisor Strategy
読む → 判断 → 必要分のみ実行。W3 = 著者 entity 強化、HCU「Anonymous content losing ground」軸の解消 + AI Overview citation 先行優位狙い。**AdSense 3rd reapply の主因 fix ではない**ことを認識、W5+W2 完走後の reapply を前提とする補強施策。

**Sprint priority**: Active Sprint Queue #9 (緊急度↓、W6-quick → W5 → W2 完走後でも OK)。

---

## 必読 memory
- ★★★ `feedback_takapon_pseudonym` (2026-05-18 REVERSAL 版)
- ★★★ `project_jpn_adsense_reject_slug_20260518` (reject 主因確定、W3 と無関係)
- ★★★ `project_jpn_current_state_20260518` (SoT)
- ★★★ `feedback_in_session_critic_not_external`
- ★★★ `feedback_critic_finding_no_deferral`
- ★★ `feedback_prompt_design_pdca`
- ★★ `feedback_handoff_doc_skepticism`
- ★★ `feedback_url_http_verify_before_handoff`
- ★ `feedback_push_before_sprint_closure`
- ★ `feedback_no_first_person_fabrication`
- ★ `feedback_wildcard_redirect_ban`

## HEAD baseline
`6e8c919` (R18 closure) + W6-quick 完走後 SHA (W3 は W6-quick の後に投入推奨、Active Queue #0→#9 順)

---

## 目的 + non-目的

**目的**:
1. 全 author 表記を `Takapon` → `Takashi Kiyohara` に atomic 一括切替
2. Person schema sameAs に LinkedIn URL 追加 + jobTitle/worksFor/knowsAbout 拡張
3. 専用 author hub page `/about/takashi-kiyohara/` 新設
4. /editorial-policy + /corrections page 新設 (AuthorBox との bidirectional trust loop)
5. AuthorBox に LinkedIn icon link 追加

**non-目的**:
- ❌ AdSense reject「有用性の低いコンテンツ」直接 fix (これは W5+W2 で対応)
- ❌ indexed 数 短期改善 (HCU entity 再評価 4-12 週、W6-quick の technical fix の方が即効)
- ❌ AI アバター生成 (memory `feedback_image_strict_universal_rule` 整合、実画像 LinkedIn 写真採用)

---

## Scope (2 commit、同 PR squash merge、Vercel 1 atomic deploy)

### Commit A (Identity SSoT 切替、約 5 file edit + 1 file rename)

#### A-1 `lib/author.ts`
```ts
// SSoT for the site author (Takashi Kiyohara, real name + LinkedIn linked since 2026-05-18).
// 旧 Takapon 仮名運用は 2026-05-18 で終了、entity verification 強化のため実名化。

export const LINKEDIN_VANITY = 'takashi-kiyohara111222';  // CI gate 用 const、typo 事故防止

export const AUTHOR = {
  name: 'Takashi Kiyohara',
  jobTitle: 'Founder & Editor',
  avatar: '/images/author/takashi-kiyohara.webp',
  avatarAlt: 'Portrait of Takashi Kiyohara, Founder & Editor of Japan Pop Now',
  profilePath: '/about/takashi-kiyohara/',
  bio: 'Kyoto-born, Tokyo-based writer covering anime, collab cafes, pilgrimage spots, and pop culture travel for international visitors. Former US strategy consultant; currently completing a graduate degree in International Relations in the UK.',
  // bio 内容は user 承認済 (旧 Takapon 時)、LinkedIn 整合、fab risk なし
  tagline: "Kyoto-born, Tokyo-based writer covering anime, collab cafes, and pop culture travel. Former US strategy consultant, currently completing a graduate degree in International Relations in the UK. Verifies every cafe and event against the operator's official source.",
  bioShort: 'Japan-based writer covering anime pop culture, collab cafes, and travel for international visitors.',
  locationLine: 'Kyoto-born, Tokyo-based. Ex-US strategy consulting, now completing a graduate degree in International Relations in the UK.',
  expertise: [
    'Anime Collab Cafes',
    'Tokyo Pop Culture Districts',
    'Content Strategy',
    'SEO',
  ],
  knowsAbout: [
    'Anime',
    'Manga',
    'Japanese Pop Culture',
    'Travel in Japan',
    'Tourism',
    'Anime Locations',
  ],
  socials: {
    threads: 'https://www.threads.net/@pop_now_jp',
    x: 'https://x.com/pop_now_jp',
    linkedin: `https://www.linkedin.com/in/${LINKEDIN_VANITY}`,
  },
} as const;

export const AUTHOR_SAME_AS: readonly string[] = [
  AUTHOR.socials.linkedin,
  AUTHOR.socials.threads,
  AUTHOR.socials.x,
] as const;
```

#### A-2 file rename + CI gate
```bash
# Rename existing real photo (user 承認済、内容据え置き)
git mv public/images/author/takapon.webp public/images/author/takashi-kiyohara.webp

# OR if user provided new LinkedIn photo (preferred):
# - user が K10_Japan Pop Now/ folder に takashi-kiyohara-linkedin.jpg を配置済なら:
sharp public/images/author/takashi-kiyohara-source.jpg -resize 512x512 -toFormat webp -quality 90 -o public/images/author/takashi-kiyohara.webp
```

★ **avatar 写真は 2 path**:
- **path A (即可)**: 既存 `takapon.webp` を rename only
- **path B (推奨)**: user が LinkedIn 写真の JPG を K10 folder に配置 → Code が WebP 変換 + 256x256-512x512 リサイズ → push

Code session 投入時に user に確認 ("/images/author/ に新写真ある? path A or B?")。

#### A-3 LinkedIn URL CI gate
新規 script `scripts/ci/verify-linkedin-url.mjs`:
```js
import { LINKEDIN_VANITY } from '../../lib/author.js';
const url = `https://www.linkedin.com/in/${LINKEDIN_VANITY}`;
const res = await fetch(url, { headers: { 'User-Agent': 'Mozilla/5.0' } });
// LinkedIn login wall で 302 を返すが、200/302 ならスラッグ有効、404 なら typo
if (res.status >= 400 && res.status < 500 && res.status !== 401) {
  console.error(`LinkedIn vanity URL invalid: ${url} -> ${res.status}`);
  process.exit(1);
}
console.log(`LinkedIn URL ${url}: ${res.status} OK`);
```
`package.json` の `scripts.lint` or `pre-push` hook に組込み。

#### A-4 AuthorBox (`components/AuthorBox.tsx`)
- 既存 `AUTHOR` 参照のみ、name + avatar 自動更新
- 追加: LinkedIn icon link
```tsx
{AUTHOR.socials.linkedin && (
  <a
    href={AUTHOR.socials.linkedin}
    target="_blank"
    rel="me noopener noreferrer"
    aria-label="LinkedIn profile"
  >
    <LinkedInIcon size={16} />
  </a>
)}
```

#### A-5 `lib/structured-data.ts` (Person JSON-LD)
- `AUTHOR_SAME_AS` 参照確認
- `worksFor: { "@id": "https://www.japan-pop-now.com/#organization" }` 追加 (Critic G-1 + Research finding)
- `image: AUTHOR.avatar` 絶対 URL 化 (`https://www.japan-pop-now.com${AUTHOR.avatar}`)

#### A-6 `lib/seo.ts`
SSoT 経由なので変更不要、AUTHOR.name で article:author meta 自動更新確認のみ。

#### A-7 `app/about/page.tsx`
- TEAM 配列 name='Takashi Kiyohara'、role + 1 行 + "Full bio →" link to `/about/takashi-kiyohara/`
- bio 詳細は author hub に集約、/about は overview のみ (overlap <20%)

#### A-8 `app/llms.txt/route.ts` + `app/llms-full.txt/route.ts`
- Author 行 = `AUTHOR.name` 経由 (hardcode あれば SSoT 化)
- URL = `${AUTHOR.profilePath}` = `/about/takashi-kiyohara/`

#### A-9 grep verify (commit 前必須)
```bash
grep -rE "Takapon" \
  --include="*.ts" --include="*.tsx" --include="*.md" --include="*.mdx" --include="*.json" \
  --exclude-dir={.git,node_modules,.next,docs,scripts/audit} \
  app/ lib/ components/ content/ public/ 2>&1 | grep -v "^Binary"
# expected: 0 hit (GiscusComments の repo 名は 'Takashi-Kiyohara/' で別 match)
```

#### Commit A message
```
fix(identity): flip author byline Takapon → Takashi Kiyohara + LinkedIn sameAs (W3 commit A)

SSoT (lib/author.ts) で AUTHOR.name + avatar + LinkedIn socials 追加。
全 article HTML (article:author meta / JSON-LD author.name / AuthorBox display) が
SSoT 経由で自動更新。avatar は実画像 rename (or LinkedIn 写真新規 WebP)。

W3 = EEAT 単独補強、AdSense reject「有用性の低いコンテンツ」直接 fix ではない
(主因は W5+W2 で対応、W3 は HCU「Anonymous content losing ground」軸の解消)。

Round 1 research: docs/prompt-design/w3-r1-research-20260518.md
Round 2 critic: docs/prompt-design/w3-r2-critic-20260518.md
Round 3 critic: docs/prompt-design/w3-r3-critic-20260518.md
Round 3 SHIP: docs/prompt-design/w3-r3-draft3-SHIP-20260518.md

Commit B (3 新 routes) 連続 push、entity confusion 回避のため同 PR squash merge。
```

### Commit B (3 新 routes)

#### B-1 `app/about/takashi-kiyohara/page.tsx` (author hub)
- title: "Takashi Kiyohara — Founder & Editor, Japan Pop Now"
- canonical: `https://www.japan-pop-now.com/about/takashi-kiyohara/`
- Breadcrumb: Home > About > Takashi Kiyohara
- 内容:
  - Hero: avatar (256×256) + AUTHOR.name + AUTHOR.jobTitle + AUTHOR.tagline
  - **How I report**: AUTHOR.tagline の "Verifies every cafe and event against the operator's official source" + editorial-policy 抜粋
  - **Topics I cover**: AUTHOR.knowsAbout 人間語展開 (3-4 段落)
  - **Recent articles**: latest 6 article へ link (内部 link graph 強化)
  - **Connect**: LinkedIn / Threads / X (icon link grid)
- Person JSON-LD render (`AUTHOR_SAME_AS` + `worksFor` で homepage Org @id 参照)

#### B-2 `app/editorial-policy/page.tsx` (Critic R-4 修正 = stub から拡張)
- title: "Editorial Policy — Japan Pop Now"
- canonical: `/editorial-policy/`
- ★ **各 section ≥ 150 words** (Critic R-4 RED 修正、thin content gate 抵触回避):

**Visit policy (≥ 150 words 例)**:
> "Whenever possible, the Japan Pop Now editor (Takashi Kiyohara) visits collab cafes, pop-up events, and venue locations firsthand to verify operating hours, menu items, pricing, and atmospheric details. Recent firsthand-reported coverage includes [link: One Piece Cafe Gene Shibuya / Chiikawa Bakery Harajuku / LuvLab Harajuku DIY Accessory Experience / Krispy Kreme Mario Galaxy / JOJO Stone Ocean Cafe Jojo World / DBZ Marugame Seimen]. For events outside reach (regional collab in Osaka/Kyushu, time-sensitive pop-ups overlapping with travel constraints), we cite operator-official sources verbatim, label image provenance (Wikimedia/press kit/AI-generated mockup) inline, and avoid first-person fabrication. Articles marked with 'firsthand' badge indicate confirmed visit; unmarked articles rely on operator-official source documentation."

**Image source policy (≥ 150 words 例)**:
> "Hero images and body images on Japan Pop Now follow a 4-tier provenance hierarchy: (1) First-party photography by Takashi Kiyohara or Japan Pop Now field team, credited as 'Photo: Japan Pop Now'; (2) Wikimedia Commons + Creative Commons licensed images, credited with photographer attribution and CC license link inline; (3) Operator press kit images obtained through official press contact channels, credited as 'Image: [operator name] press kit'; (4) AI-generated mockups for visual concepts where no licensable image exists, credited as 'Mockup: AI-generated illustration'. We never use Pinterest screenshots, Aniplex/Getty proprietary images without license, or photographer-watermarked images. All image credits are visible adjacent to the image or in the article footer."

**AI policy (≥ 150 words 例)**:
> "Japan Pop Now uses AI tools (primarily Claude Sonnet) to assist with translation between Japanese source materials and English published articles, to draft initial article framing from operator-official source pages, and to perform image format conversion (WebP optimization, face-aware cropping). All factual claims — prices, hours, addresses, dates, menu items — are verified against the operator's official Japanese-language source by the editor before publication. AI output is treated as draft material requiring human editorial pass; we do not publish AI-generated content without editorial verification. AI-generated images are explicitly labeled (see Image source policy). This policy aligns with Google Search Central's 'helpful content' principles: AI assistance does not exempt content from the human-editor accountability standard."

- 内部 link grid: [About author](/about/takashi-kiyohara/) / [Corrections](/corrections/)
- footer link grid に追加

#### B-3 `app/corrections/page.tsx`
- title: "Corrections — Japan Pop Now"
- canonical: `/corrections/`
- 内容:
  - "We correct errors promptly. Report typos, outdated information, or factual mistakes via [snsganbaro@gmail.com](mailto:snsganbaro@gmail.com)."
  - 訂正履歴 section: 空 template (frontmatter `lastUpdated: ${ISO}` auto-populate 設計、現状空 OK)
- 内部 link grid: [About author](/about/takashi-kiyohara/) / [Editorial policy](/editorial-policy/)
- footer link grid に追加

#### B-4 共通: sitemap + robots + footer link
- `app/sitemap.ts` (or 該当 generator) に 3 page URL 追加 (lastmod = 今日付)
- robots meta = "index, follow" (default)
- footer link grid (`components/Footer.tsx` 等) に新 3 page 追加 (orphan 防止、Critic B-4 mitigated)

#### B-5 grep verify
```bash
GBOT="Mozilla/5.0 (compatible; Googlebot/2.1; +http://www.google.com/bot.html)"
curl -sA "$GBOT" "https://www.japan-pop-now.com/sitemap.xml" | grep -E "(takashi-kiyohara|editorial-policy|corrections)"
# expected: 3 hits
```

#### Commit B message
```
feat(eeat): add author hub + editorial policy + corrections page (W3 commit B)

新規 3 routes:
- /about/takashi-kiyohara/ — Person profile hub (Person JSON-LD, knowsAbout 展開, recent articles)
- /editorial-policy/ — Visit/Image source/AI policy 各 ≥150 words (Critic R-4 RED 反映)
- /corrections/ — 訂正受付窓口 (snsganbaro@gmail.com)

sitemap + footer link 同 commit、orphan 防止。
AuthorBox link grid から bidirectional trust loop。

Commit A (SSoT flip) と同 PR squash merge、Vercel 1 atomic deploy。
```

### PR 設定
- "**Squash and merge**" 選択 (Critic Y-1 修正、Vercel deploy 1 atomic)
- Co-Authored-By なし (Takashi 単独 commit)

---

## Phase 4: live verify (push 後 1 分 wait)

### 4-1 Commit A SSoT 反映確認
```bash
GBOT="Mozilla/5.0 (compatible; Googlebot/2.1; +http://www.google.com/bot.html)"

# article HTML 3 hit 全更新
curl -sA "$GBOT" "https://www.japan-pop-now.com/articles/chiikawa-bakery-harajuku-guide-2026" | grep -oE '(article:author content="[^"]+"|"name":"[^"]+","url":"[^"]+about[^"]*"|takashi-kiyohara\.webp)'
# expected: article:author content="Takashi Kiyohara", "name":"Takashi Kiyohara", takashi-kiyohara.webp

# sameAs に LinkedIn URL
curl -sA "$GBOT" "https://www.japan-pop-now.com/articles/chiikawa-bakery-harajuku-guide-2026" | grep -oE 'linkedin\.com/in/takashi-kiyohara111222'
# expected: 1+ hit

# avatar 画像 200
curl -sIA "$GBOT" "https://www.japan-pop-now.com/images/author/takashi-kiyohara.webp" | head -1
# expected: HTTP/2 200

# 旧 avatar 404 (rename 確認)
curl -sIA "$GBOT" "https://www.japan-pop-now.com/images/author/takapon.webp" | head -1
# expected: HTTP/2 404

# live Takapon hit 0
for p in / /about /articles/chiikawa-bakery-harajuku-guide-2026; do
  echo "=== $p ==="
  curl -sA "$GBOT" "https://www.japan-pop-now.com${p}" | grep -ic takapon
done
# expected: all 0
```

### 4-2 Commit B 3 page HTTP 200 + Person JSON-LD
```bash
GBOT="Mozilla/5.0 (compatible; Googlebot/2.1; +http://www.google.com/bot.html)"

for p in about/takashi-kiyohara/ editorial-policy/ corrections/; do
  status=$(curl -sIA "$GBOT" "https://www.japan-pop-now.com/$p" | head -1)
  echo "$p -> $status"
done
# expected: all HTTP/2 200

# author hub Person JSON-LD
curl -sA "$GBOT" "https://www.japan-pop-now.com/about/takashi-kiyohara/" | grep -oE '"@type":"Person"[^}]*"name":"Takashi Kiyohara"' | head -1
# expected: 1 hit

# editorial-policy + corrections word count
for p in editorial-policy corrections; do
  wc=$(curl -sA "$GBOT" "https://www.japan-pop-now.com/$p" | wc -w)
  echo "$p -> $wc words"
done
# expected: editorial-policy ≥ 500 (150 × 3 sections + boilerplate), corrections ≥ 100

# footer link 3 page 含む
curl -sA "$GBOT" "https://www.japan-pop-now.com/" | grep -ocE "(about/takashi-kiyohara|editorial-policy|corrections)"
# expected: 3+ hits
```

### 4-3 schema.org validator
- `https://validator.schema.org/#url=https://www.japan-pop-now.com/about/takashi-kiyohara/` で Person + Organization JSON-LD validity
- 期待: 両 GREEN

### 4-4 rel=me reciprocal (post-push manual task)
- user に LinkedIn プロフィール → Contact info → ウェブサイト欄に `https://www.japan-pop-now.com/about/takashi-kiyohara/` 追加依頼 (Critic Y-6)

---

## Rollback 設計
- atomic single PR revert で済む
- Google cache 旧 Takapon entity 4-12 週残存期間は GSC URL Inspection で:
  - /about + /about/takashi-kiyohara/ + /editorial-policy/ + /corrections/ + homepage = 5 URL 手動 re-index (Critic Y-9 修正、5/日 quota 内)
  - article 群は IndexNow plugin に委譲

---

## Code 投入直前 Cowork checklist

1. `lib/author.ts` 内 `LINKEDIN_VANITY` const が `"takashi-kiyohara111222"` 一致確認
2. `scripts/ci/verify-linkedin-url.mjs` 動作確認、LinkedIn URL 200/302 OK
3. /editorial-policy/ 3 sections 各 ≥150 words 確認 (`wc -w` gate)
4. `grep -rE "Takapon" --include="*.{ts,tsx,md,mdx,json}" --exclude-dir={.git,node_modules,.next,docs,scripts/audit}` 0 hit verify
5. PR settings = "Squash and merge"、Co-Authored-By なし
6. avatar 画像 path 確定 (path A rename / path B 新写真 WebP 変換 / user 承認)
7. Vercel preview URL で /about/takashi-kiyohara/ Person JSON-LD を Schema.org validator pass

---

## 報告 format (Code → Cowork)

```
W3 identity flip 完了:
- HEAD: <new SHA>  (origin/main 一致、W6-quick の後 baseline からの差分)
- Single PR (squash merge): #N
- Commit A SSoT flip: <sha>
- Commit B 3 新 routes: <sha>
- 変更 files: lib/author.ts, components/AuthorBox.tsx, lib/structured-data.ts, app/about/page.tsx, app/llms.txt/route.ts, app/llms-full.txt/route.ts, public/images/author/takashi-kiyohara.webp (rename or new), app/about/takashi-kiyohara/page.tsx (new), app/editorial-policy/page.tsx (new), app/corrections/page.tsx (new), app/sitemap.ts, components/Footer.tsx, scripts/ci/verify-linkedin-url.mjs (new)
- in-session Critic: <agentId> + verdict
- Phase 4 verify (4 step 結果 verbatim):
  - 4-1 SSoT 反映 article:author = "Takashi Kiyohara", JSON-LD name = "Takashi Kiyohara", sameAs linkedin URL present, avatar new path 200, Takapon live hit 0
  - 4-2 3 new pages HTTP 200, Person JSON-LD render, editorial-policy ≥500 words, footer link 3 page 含む
  - 4-3 schema.org validator: Person + Org 両 GREEN
- Cowork external verify queue: 24h / 7d / 30d
- user manual task: LinkedIn Contact info に reciprocal link 追加 (post-push 1 週間以内)
```

---

★ **本 Draft 3 = ship 版**、Code 投入準備完了 (W6-quick 完走後の sprint queue #9 で投入)
