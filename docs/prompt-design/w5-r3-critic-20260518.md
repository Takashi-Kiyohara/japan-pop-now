# W5 Round 3 — Light Critic (2026-05-18)

**Subagent**: a255ed9d16a0a9820 (general-purpose、43 秒、35k tokens)
**Verdict**: 🟢 GREEN (convergent) — SHIP 認定可、Draft 3 軽 patch 5 項目

## 主要 verdict

| 項目 | verdict | 理由 |
|---|---|---|
| E 軸 composite + calibration + GPTZero | 🟢 | ESL FP 61.3% を 8-12% に圧縮可能 |
| 維持 ≥6 + A∧F∧G mandatory | 🟢 | HCU EEAT 論文と整合、適正 |
| B 軸 press 不在 fallback | 🟡 | competitor scraping 自体 fail edge case で「fallback の fallback」未定義 |
| CI gate yml | 🟢 | leak loop 完全防止 |
| niche whitelist 17 phrase | 🟡 | 8 phrase 不足、co-occur condition 未指定 |
| delete routing 2-stage | 🟡 | event-bound + supersededBy edge case、90 日 noindex TTL 未指定 |

## Draft 3 軽 patch 5 項目

### P-1 B 軸 scraping fail fallback 1 行
competitor top10 fetch も fail した場合、`B = null` + `manual_SME_flag = true` 設定、user 個別判断列追加。

### P-2 niche whitelist 25 phrase 拡張 + co-occur 条件
追加 8 phrase: `otaku culture` / `kawaii aesthetic` / `oshi (推し)` / `seichi junrei (聖地巡礼)` / `gachapon` / `merch drop` / `season-limited` / `itasha`
除外候補: `ramen` `matcha` (普通名詞、boilerplate 混同低、whitelist 不要)
警戒: `pilgrimage` は `fan pilgrimage` 形でのみ pass、単体は boilerplate
各 phrase に `co_occur_with: ["IP", "location"]` 条件付与

### P-3 delete routing 2-stage → 3-stage
```
stage A (event-bound): endDate < today - 60d AND category in [collab-cafe/popup/limited-event] → 410
stage B (supersededBy): stage A FALSE AND frontmatter.supersededBy != null → 301 (link equity > canonical 汚染、Mario Cafe 2025→2026 precedent)
stage C (default): stage A,B FALSE → noindex with **90日 TTL**、grace 期間中に書き直し未実施なら stage A 410 自動昇格 (orphan noindex の HCU re-degradation 防止)
```

event-bound + supersededBy = **stage B 優先** (link equity > canonical 汚染)
manual gate bottleneck 緩和: 「同一 IP × 同一店舗 × ±90日」auto-shortlist で 80% 自動化、残 manual 15分/件 × 15 = 4h 許容

### P-4 GPTZero 予算化 or suspect 圧縮
- option a: Pro $10/月 (500k token) を `.env.local` で API key 入手、month 5/月 予算追加
- option b: suspect zone 30% → **15%** に絞り 45k token に圧縮、free 10k/月では足りないが Pro 不要
- Draft 3 では **option b 採用** (cost minimize、Pro は再 reject 時 escalate)

### P-5 Phase 0 press_sources 準備独立化
S3 内自動収集は WebSearch hallucination risk 高、`docs/audit/w5-press-sources.json` を **Cowork 事前準備 4-6h 独立化**。各 slug × press URL verbatim、WebFetch で 200 確認必須。

## 残 blind spot 補足

- calibration set 5 article: firsthand 確認必要、user 撮影記事 (one-piece-cafe-gene / chiikawa-bakery / luvlab / dragon-ball-marugame / jojo-stone-ocean) で確定済、Draft 3 で slug 明示
- GPTZero free tier 不足は P-4 で対応
- press_sources 準備は P-5 で対応

## ★ Overall: 🟢 GREEN (convergent)

R2 RED 4 項目全 GREEN/軽 YELLOW、blocker なし。framework 構造 SHIP-ready。Draft 3 = 5 軽 patch 反映で **W5 sprint launch OK**。
