# R11 — HOLD on Phase 0 (video pipeline blockers)

**Date:** 2026-05-10
**Status:** HOLD — waiting on user-side video pipeline setup
**Authority:** User-approved via AskUserQuestion (Q1 = "STOP — wait for video pipeline"; Q2 = "Hybrid — use existing 22 memories, document missing 18+")
**Decision recorded by:** Code (R11 Phase 0 capability check, this session)

## Why HOLD (verified blockers)

### Hard capability gaps (verified 2026-05-10)
1. **`ffmpeg` not in PATH** — `which ffmpeg` returns no result on this Windows shell. Cannot extract frames via the spec's `ffmpeg -i {video} -vf "select='gt(scene,0.3)'..."` command.
2. **No live v4.3 face-detection pipeline** — only `scripts/gdrive_video_poll.py.deprecated` exists; no working code for Moe-detect-confidence-≥0.85 + `__moe` prefixing, no OCR `__readable` prefixing.
3. **GDrive video binaries not on local filesystem** — the 11 GDrive folder URLs listed in the prompt resolve to remote folders. Available `mcp__claude_ai_Google_Drive__*` tools cover metadata + text-file content; useful video-binary download to local disk is not on the toolchain.
4. **No 8-variant image generation pipeline** — RULE K requires hero/body/portrait/og/twitter/card/schema-1×1/schema-4×3 generation per frame. Even with frames in hand, the WebP q=92 + UnsharpMask + face-aware crop chain isn't materialized.

### Asserted-but-missing infrastructure
The R11 prompt names **18+ ★最重要 memory files** as required reading; only `project_image_procurement_20260422` exists in `C:\Users\user\.claude\projects\C--Users-user-OneDrive--------GitHub-japan-pop-now\memory\`. Missing:

- feedback_overclaim_treadmill_pattern (★★★)
- feedback_in_session_critic_not_external (★★★)
- feedback_critic_finding_no_deferral (★★★)
- feedback_critic_full_layer_checklist (★★★)
- feedback_image_strict_universal_rule (★★)
- feedback_image_quality_standards (★★)
- feedback_image_text_matching (★★)
- feedback_official_image_modification_ok (★★)
- feedback_image_claim_verify_strict (★★)
- feedback_takapon_pseudonym (★★)
- feedback_no_first_person_fabrication (★★)
- feedback_moe_detection (★)
- project_jpn_video_pipeline_v43 / v5 (★)
- feedback_no_stub_articles (★)
- feedback_url_slug_verify (★)
- feedback_threads_not_instagram (★)
- feedback_bare_klook_url_ban (★)
- feedback_url_http_verify_before_handoff (★)

Reference docs also missing:
- `BRUTAL_AUDIT_20260509_BLOCKERS.md` (4th sprint asserting; documented in `feedback_master_sprint_pattern`)
- `project_jpn_real_photo_articles_20260509.md`

This is the same pattern documented in active memory `feedback_master_sprint_pattern` ("user references memories and source-of-truth docs that don't exist as tracked files... discipline of saying so is what prevents premise-based work").

### RULE G structural conflict
RULE G ("session <8h = STOP") directly contradicts active user-written memory `feedback_master_sprint_pattern`:

> "The asserted '8-12h autonomous' or '12-16h autonomous' framing is aspirational. Realistic single-Claude-session output is 1-3h of focused work + critic + docs. Beyond that, fixes get manufactured to fill the time, surfacing diminishing returns."

The 8h floor structurally incentivizes fabrication when real work runs out — the exact failure mode R10 (and R11) explicitly aim to prevent. This was surfaced via AskUserQuestion before any fabrication could occur.

## Memory-handling decision (user-approved)
Apply the **22 memories that DO exist** in MEMORY.md (R10's hybrid pattern). Surface the 18+ asserted-but-missing files as gap in this doc. Do not fabricate or extend.

## Restart conditions (when these are true, R11 can resume from Phase 0)

To make Phase 0 (video → frame catalog) actually executable, the following must land BEFORE re-launching R11:

1. **`ffmpeg` installed + on PATH** — `which ffmpeg` returns a path on Windows shell.
2. **Videos downloaded to local disk** — the 11 GDrive folders' MP4/MOV files resident under e.g. `local_aeb02443-9532-40c2-9cd0-aec0b435df27/uploads/r11-videos/{slug}/{video}.mp4`.
3. **v4.3 face-detection pipeline materialized** — either the existing `.deprecated` script revived OR a new `scripts/r11-frame-extract.py` (or .mjs) capable of: scene-detect frame extract → 8-variant generation (hero/body/portrait/og/twitter/card/schema-1×1/schema-4×3 at the spec resolutions) → face-detect with `__moe` prefix → OCR with `__readable` prefix.
4. **The 18+ ★最重要 memories actually exist** — either user materializes them as `.md` files under the memory directory, OR the R11 v2 prompt drops the references and uses only memories that exist (the R10 pattern that worked).

Optional but recommended:
5. RULE G softened or removed — duration is not a quality metric; the work product is. Active memory `feedback_master_sprint_pattern` explains why.

## What's NOT blocked (could be done independently if desired)

- **Klook rel=sponsored sweep** — the workflow's Check 4 currently verifies `aff_adid` only; the affiliate rule also requires `rel="nofollow sponsored noopener"`. Per R10-tail commit `111f85c`, only `best-anime-tours-tokyo-2026.md` is rel-compliant; the other 11 articles with klook markdown links lack `rel`. This is a 1-2h sweep + workflow-Check-5 add. Independent of R11 video pipeline.
- **WebSearch-only article research** — could research the 9 new article topics (animate-shinjuku, dragon-ball-marugame, etc.) without writing them. Output = research dossiers in `docs/research/`. Useful when the video pipeline lands and articles need to be drafted.
- **Existing-article rel-sponsored sweep** — same R10-tail pattern applied to remaining 11 articles. Per Critic R3 a9aeb7a31d7f9fe84 R10 closed at editorial 70-78%; rel sweep would harden FTC compliance further independent of new content.

These are NOT auto-started. User decision required before launching any of them.

## End state of this turn

- R11 Phase 0 NOT started (no video access, no asserted infrastructure)
- This HOLD doc landed (not yet committed; awaiting user decision on commit + on what to do next)
- No fabrication produced
- R10 closeout (commit `a99c98b`) remains the latest authoritative session
