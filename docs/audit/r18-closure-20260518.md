---
round: R18
date: 2026-05-18
baseline_sha: e2e2625
status: pending-post-deploy-verify
---

# R18 Closure Record — scope reality-check + P3/P4/P5 + YELLOW

## Handoff vs. verified reality

The resume handoff said "baseline confirmed, no re-verify needed". Verification
(git history + live `curl`) found three of its premises inaccurate. Re-verifying
was correct, not redundant.

| Item | Handoff premise | Verified reality | Action |
|---|---|---|---|
| P4 | noindex articles listed; "4 RED"; index 88 vs sitemap 70 is a separate issue | **18/18** noindex articles present in the live 88-link index. Correct filter → 88→70, which equals the sitemap exactly (so it also closes the "差14") | Implemented |
| P3 | 94 raw klook links; dual render path via `lib/markdown-utils.ts` | `markdown-utils.ts` has no render pipeline — **single** path (`ArticleBody`→MDXRemote). But 83 raw markdown klook links at source; **31/91** klook anchors missing `sponsored` in a 25-article live scan | Implemented (single-path) |
| P5 | middleware Vary append is correct; vercel.json Vary is dead config, delete it | git: middleware Vary (`4ca138e`) was already tried and **reverted** by `ba282a1`; live proves **both** approaches fail — prod Vary is only `rsc, next-router-*` on every UA | Re-scoped (see below) |

## P4 — exclude noindex articles from /articles index

`app/articles/page.tsx`: filter `getAllArticles()` by `!robots.includes('noindex')`.
Same filtered set feeds the grid, category counts, the `{n} articles` label, and
the CollectionPage ItemList JSON-LD, so all stay consistent. Expected live:
index 88 → 70 (= sitemap), all 18 noindex slugs absent from the index.

## P3 — affiliate rel=sponsored (single render path)

New `lib/rehype-affiliate-rel.ts`: domain-scoped rehype plugin that Set-merges
`rel="sponsored nofollow noopener noreferrer"` onto affiliate-network anchors.
Wired into `ArticleBody.tsx` **after** `rehype-external-links` (external-links
*replaces* `rel`, so order matters). Domain-scoped (not "sponsored on all
external links") so journalistic citations are not mis-tagged as paid — an
incorrect FTC signal. The `<AffiliateCTA>`/inline-HTML links already carried
`sponsored`; this backfills only the raw-markdown-link case. CI guard:
`scripts/ci/affiliate-rel-check.ts` (`npm run affiliate-check`) — asserts plugin
behavior + wiring/order; runs in the gate.

## P5 — re-scoped: fix the cache-poisoning risk, not Vary

Both documented Vary mechanisms are proven dead on Vercel + Next 16 RSC
(`4ca138e` middleware, `ba282a1` vercel.json). Rather than re-introduce the
reverted regression, the actual risk Vary was meant to mitigate is handled at
its source: the **only** UA-variant output is `X-Robots-Tag: noindex` set for
unknown bots. `middleware.ts` now also sets `Cache-Control: private, no-store`
on exactly that response, so Vercel's shared CDN never stores the noindex
variant and serves it to a human (or vice versa). `vercel.json` Vary line left
in place (harmless no-op); the misleading "vercel.json composes correctly"
comment corrected to match live evidence.

## YELLOW residue — deferred with rationale (not fabricated, not churned)

Residual YELLOW (per `scripts/r16/per-article-quality-audit.py`):
`detective-conan-cafe-2026-japan-guide` (11), `japan-rail-pass-guide-anime-fans`
(10), `slam-dunk-kamakura-pilgrimage-2026` (11), `universal-cool-japan-2026-guide`
(11).

**All four already carry `robots: "noindex,follow"`.** They are de-indexed,
sitemap-excluded, and now (P4) de-listed from `/articles`. They present zero
HCU / index-quality risk. Raising their score would require either fabricated
firsthand experience (forbidden — author has not visited; see auto-memory
`feedback_no_first_person_fabrication`) or new verified field reporting (no new
verified facts available this session). Per the handoff's own `c13/c14 → defer
明示` contingency, the residual is **structurally mitigated, not content-fixed**.
Explicitly deferred, not silently dropped.

## Known pre-existing conditions (NOT introduced by R18, out of scope)

- `npm run lint` exits 1 due to 3 pre-existing `@typescript-eslint/no-require-imports`
  errors in `app/menu/page.tsx`, `components/CookieConsent.tsx`,
  `lib/affiliate-map.ts` — present at baseline `e2e2625`, untouched by R18.
  Scoped lint of the 5 R18-changed files: 0 errors. Not fixed here to avoid
  unreviewed scope-creep into unrelated files.
- Working tree at `e2e2625` already had unrelated uncommitted/untracked files
  (`tmp/`, `components/ArticleCardV2.tsx`, `docs/audit/full-corpus-*`, etc.).
  R18 commits use targeted `git add` only; these are left as found.

## Gate results (pre-deploy)

- `npm run build`: exit 0
- `npx tsc --noEmit`: exit 0
- `npm run affiliate-check`: PASS
- `npm run lint`: exit 1 — pre-existing only (see above); 0 errors in R18 files

Post-deploy live verification + external Critic recorded on completion below.
